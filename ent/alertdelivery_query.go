// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/Phoenix-Uptime/phoenix-go/ent/alertdelivery"
	"github.com/Phoenix-Uptime/phoenix-go/ent/alertrule"
	"github.com/Phoenix-Uptime/phoenix-go/ent/incident"
	"github.com/Phoenix-Uptime/phoenix-go/ent/monitor"
	"github.com/Phoenix-Uptime/phoenix-go/ent/notificationchannel"
	"github.com/Phoenix-Uptime/phoenix-go/ent/predicate"
)

// AlertDeliveryQuery is the builder for querying AlertDelivery entities.
type AlertDeliveryQuery struct {
	config
	ctx                     *QueryContext
	order                   []alertdelivery.OrderOption
	inters                  []Interceptor
	predicates              []predicate.AlertDelivery
	withAlertRule           *AlertRuleQuery
	withNotificationChannel *NotificationChannelQuery
	withMonitor             *MonitorQuery
	withIncident            *IncidentQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the AlertDeliveryQuery builder.
func (_q *AlertDeliveryQuery) Where(ps ...predicate.AlertDelivery) *AlertDeliveryQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *AlertDeliveryQuery) Limit(limit int) *AlertDeliveryQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *AlertDeliveryQuery) Offset(offset int) *AlertDeliveryQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *AlertDeliveryQuery) Unique(unique bool) *AlertDeliveryQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *AlertDeliveryQuery) Order(o ...alertdelivery.OrderOption) *AlertDeliveryQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryAlertRule chains the current query on the "alert_rule" edge.
func (_q *AlertDeliveryQuery) QueryAlertRule() *AlertRuleQuery {
	query := (&AlertRuleClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(alertdelivery.Table, alertdelivery.FieldID, selector),
			sqlgraph.To(alertrule.Table, alertrule.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, alertdelivery.AlertRuleTable, alertdelivery.AlertRuleColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryNotificationChannel chains the current query on the "notification_channel" edge.
func (_q *AlertDeliveryQuery) QueryNotificationChannel() *NotificationChannelQuery {
	query := (&NotificationChannelClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(alertdelivery.Table, alertdelivery.FieldID, selector),
			sqlgraph.To(notificationchannel.Table, notificationchannel.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, alertdelivery.NotificationChannelTable, alertdelivery.NotificationChannelColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryMonitor chains the current query on the "monitor" edge.
func (_q *AlertDeliveryQuery) QueryMonitor() *MonitorQuery {
	query := (&MonitorClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(alertdelivery.Table, alertdelivery.FieldID, selector),
			sqlgraph.To(monitor.Table, monitor.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, alertdelivery.MonitorTable, alertdelivery.MonitorColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryIncident chains the current query on the "incident" edge.
func (_q *AlertDeliveryQuery) QueryIncident() *IncidentQuery {
	query := (&IncidentClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(alertdelivery.Table, alertdelivery.FieldID, selector),
			sqlgraph.To(incident.Table, incident.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, alertdelivery.IncidentTable, alertdelivery.IncidentColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first AlertDelivery entity from the query.
// Returns a *NotFoundError when no AlertDelivery was found.
func (_q *AlertDeliveryQuery) First(ctx context.Context) (*AlertDelivery, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{alertdelivery.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *AlertDeliveryQuery) FirstX(ctx context.Context) *AlertDelivery {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first AlertDelivery ID from the query.
// Returns a *NotFoundError when no AlertDelivery ID was found.
func (_q *AlertDeliveryQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{alertdelivery.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *AlertDeliveryQuery) FirstIDX(ctx context.Context) int {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single AlertDelivery entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one AlertDelivery entity is found.
// Returns a *NotFoundError when no AlertDelivery entities are found.
func (_q *AlertDeliveryQuery) Only(ctx context.Context) (*AlertDelivery, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{alertdelivery.Label}
	default:
		return nil, &NotSingularError{alertdelivery.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *AlertDeliveryQuery) OnlyX(ctx context.Context) *AlertDelivery {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only AlertDelivery ID in the query.
// Returns a *NotSingularError when more than one AlertDelivery ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *AlertDeliveryQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{alertdelivery.Label}
	default:
		err = &NotSingularError{alertdelivery.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *AlertDeliveryQuery) OnlyIDX(ctx context.Context) int {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of AlertDeliveries.
func (_q *AlertDeliveryQuery) All(ctx context.Context) ([]*AlertDelivery, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*AlertDelivery, *AlertDeliveryQuery]()
	return withInterceptors[[]*AlertDelivery](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *AlertDeliveryQuery) AllX(ctx context.Context) []*AlertDelivery {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of AlertDelivery IDs.
func (_q *AlertDeliveryQuery) IDs(ctx context.Context) (ids []int, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(alertdelivery.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *AlertDeliveryQuery) IDsX(ctx context.Context) []int {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *AlertDeliveryQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*AlertDeliveryQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *AlertDeliveryQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *AlertDeliveryQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *AlertDeliveryQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the AlertDeliveryQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *AlertDeliveryQuery) Clone() *AlertDeliveryQuery {
	if _q == nil {
		return nil
	}
	return &AlertDeliveryQuery{
		config:                  _q.config,
		ctx:                     _q.ctx.Clone(),
		order:                   append([]alertdelivery.OrderOption{}, _q.order...),
		inters:                  append([]Interceptor{}, _q.inters...),
		predicates:              append([]predicate.AlertDelivery{}, _q.predicates...),
		withAlertRule:           _q.withAlertRule.Clone(),
		withNotificationChannel: _q.withNotificationChannel.Clone(),
		withMonitor:             _q.withMonitor.Clone(),
		withIncident:            _q.withIncident.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithAlertRule tells the query-builder to eager-load the nodes that are connected to
// the "alert_rule" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *AlertDeliveryQuery) WithAlertRule(opts ...func(*AlertRuleQuery)) *AlertDeliveryQuery {
	query := (&AlertRuleClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withAlertRule = query
	return _q
}

// WithNotificationChannel tells the query-builder to eager-load the nodes that are connected to
// the "notification_channel" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *AlertDeliveryQuery) WithNotificationChannel(opts ...func(*NotificationChannelQuery)) *AlertDeliveryQuery {
	query := (&NotificationChannelClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withNotificationChannel = query
	return _q
}

// WithMonitor tells the query-builder to eager-load the nodes that are connected to
// the "monitor" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *AlertDeliveryQuery) WithMonitor(opts ...func(*MonitorQuery)) *AlertDeliveryQuery {
	query := (&MonitorClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withMonitor = query
	return _q
}

// WithIncident tells the query-builder to eager-load the nodes that are connected to
// the "incident" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *AlertDeliveryQuery) WithIncident(opts ...func(*IncidentQuery)) *AlertDeliveryQuery {
	query := (&IncidentClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withIncident = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		AlertRuleID int `json:"alert_rule_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.AlertDelivery.Query().
//		GroupBy(alertdelivery.FieldAlertRuleID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *AlertDeliveryQuery) GroupBy(field string, fields ...string) *AlertDeliveryGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &AlertDeliveryGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = alertdelivery.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		AlertRuleID int `json:"alert_rule_id,omitempty"`
//	}
//
//	client.AlertDelivery.Query().
//		Select(alertdelivery.FieldAlertRuleID).
//		Scan(ctx, &v)
func (_q *AlertDeliveryQuery) Select(fields ...string) *AlertDeliverySelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &AlertDeliverySelect{AlertDeliveryQuery: _q}
	sbuild.label = alertdelivery.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a AlertDeliverySelect configured with the given aggregations.
func (_q *AlertDeliveryQuery) Aggregate(fns ...AggregateFunc) *AlertDeliverySelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *AlertDeliveryQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !alertdelivery.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *AlertDeliveryQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*AlertDelivery, error) {
	var (
		nodes       = []*AlertDelivery{}
		_spec       = _q.querySpec()
		loadedTypes = [4]bool{
			_q.withAlertRule != nil,
			_q.withNotificationChannel != nil,
			_q.withMonitor != nil,
			_q.withIncident != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*AlertDelivery).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &AlertDelivery{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withAlertRule; query != nil {
		if err := _q.loadAlertRule(ctx, query, nodes, nil,
			func(n *AlertDelivery, e *AlertRule) { n.Edges.AlertRule = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withNotificationChannel; query != nil {
		if err := _q.loadNotificationChannel(ctx, query, nodes, nil,
			func(n *AlertDelivery, e *NotificationChannel) { n.Edges.NotificationChannel = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withMonitor; query != nil {
		if err := _q.loadMonitor(ctx, query, nodes, nil,
			func(n *AlertDelivery, e *Monitor) { n.Edges.Monitor = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withIncident; query != nil {
		if err := _q.loadIncident(ctx, query, nodes, nil,
			func(n *AlertDelivery, e *Incident) { n.Edges.Incident = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *AlertDeliveryQuery) loadAlertRule(ctx context.Context, query *AlertRuleQuery, nodes []*AlertDelivery, init func(*AlertDelivery), assign func(*AlertDelivery, *AlertRule)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*AlertDelivery)
	for i := range nodes {
		fk := nodes[i].AlertRuleID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(alertrule.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "alert_rule_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *AlertDeliveryQuery) loadNotificationChannel(ctx context.Context, query *NotificationChannelQuery, nodes []*AlertDelivery, init func(*AlertDelivery), assign func(*AlertDelivery, *NotificationChannel)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*AlertDelivery)
	for i := range nodes {
		fk := nodes[i].NotificationChannelID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(notificationchannel.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "notification_channel_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *AlertDeliveryQuery) loadMonitor(ctx context.Context, query *MonitorQuery, nodes []*AlertDelivery, init func(*AlertDelivery), assign func(*AlertDelivery, *Monitor)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*AlertDelivery)
	for i := range nodes {
		if nodes[i].MonitorID == nil {
			continue
		}
		fk := *nodes[i].MonitorID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(monitor.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "monitor_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *AlertDeliveryQuery) loadIncident(ctx context.Context, query *IncidentQuery, nodes []*AlertDelivery, init func(*AlertDelivery), assign func(*AlertDelivery, *Incident)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*AlertDelivery)
	for i := range nodes {
		if nodes[i].IncidentID == nil {
			continue
		}
		fk := *nodes[i].IncidentID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(incident.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "incident_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (_q *AlertDeliveryQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *AlertDeliveryQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(alertdelivery.Table, alertdelivery.Columns, sqlgraph.NewFieldSpec(alertdelivery.FieldID, field.TypeInt))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, alertdelivery.FieldID)
		for i := range fields {
			if fields[i] != alertdelivery.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if _q.withAlertRule != nil {
			_spec.Node.AddColumnOnce(alertdelivery.FieldAlertRuleID)
		}
		if _q.withNotificationChannel != nil {
			_spec.Node.AddColumnOnce(alertdelivery.FieldNotificationChannelID)
		}
		if _q.withMonitor != nil {
			_spec.Node.AddColumnOnce(alertdelivery.FieldMonitorID)
		}
		if _q.withIncident != nil {
			_spec.Node.AddColumnOnce(alertdelivery.FieldIncidentID)
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *AlertDeliveryQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(alertdelivery.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = alertdelivery.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// AlertDeliveryGroupBy is the group-by builder for AlertDelivery entities.
type AlertDeliveryGroupBy struct {
	selector
	build *AlertDeliveryQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *AlertDeliveryGroupBy) Aggregate(fns ...AggregateFunc) *AlertDeliveryGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *AlertDeliveryGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*AlertDeliveryQuery, *AlertDeliveryGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *AlertDeliveryGroupBy) sqlScan(ctx context.Context, root *AlertDeliveryQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// AlertDeliverySelect is the builder for selecting fields of AlertDelivery entities.
type AlertDeliverySelect struct {
	*AlertDeliveryQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *AlertDeliverySelect) Aggregate(fns ...AggregateFunc) *AlertDeliverySelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *AlertDeliverySelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*AlertDeliveryQuery, *AlertDeliverySelect](ctx, _s.AlertDeliveryQuery, _s, _s.inters, v)
}

func (_s *AlertDeliverySelect) sqlScan(ctx context.Context, root *AlertDeliveryQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
